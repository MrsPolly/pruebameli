import Vue from 'vue'
import Vuex from 'vuex'
import tienda from '../services/tienda'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tienda
  }
})
